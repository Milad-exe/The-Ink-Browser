/**
 * bruno-preload.js — legacy file, no longer loaded.
 *
 * The Bruno panel WebContentsView uses preload/preload.js, which exposes the
 * full window.bruno bridge and all other required APIs. This file is kept for
 * reference only and is not referenced in any window or WebContentsView.
 */
